<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ayathe.project.scheduleapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScheduleApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".home.homeactivity.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".registerlogin.activityreglog.LogRegActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".home.usersettings.UserSettings"
            android:exported="true">
        </activity>
        <activity
            android:name=".home.usersettings.ChangePersonalInfoActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".SplashScreen"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>